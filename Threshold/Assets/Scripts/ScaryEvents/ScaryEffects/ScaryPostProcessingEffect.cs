using UnityEngine;

namespace ScaryEvents.ScaryEffects
{
    public class ScaryPostProcessingEffect : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
